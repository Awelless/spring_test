spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost/sweater}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}
spring.jpa.generate-ddl=false
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=validate

spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true
spring.jpa.properties.hibernate.jdbc.non_contextual_creation=true

spring.freemarker.expose-request-attributes=true

spring.mail.host=smtp-pulse.com
spring.mail.username=awelless@mail.ru
spring.mail.password=${mailPassword}
spring.mail.port=465
spring.mail.protocol=smtps
mail.debug=false

recaptcha.secret=${captchaSecret}

spring.session.jdbc.initialize-schema=always
spring.session.jdbc.table-name=SPRING_SESSION

upload.path=https://res.cloudinary.com/nyaaansweater

cloudinary.key=591864439755628
cloudinary.secret=${cloudinarySecret}
cloudinary.environment=${cloudinaryEnvironment}

